import type { CodePreviewData } from "$/types"
import EffectType from "./Effect.type.md"

export const effectTypePreview = {
  element: EffectType,
  link: {
    text: "Effect",
    href: "/docs/api/signal#effect",
  },
} satisfies CodePreviewData
