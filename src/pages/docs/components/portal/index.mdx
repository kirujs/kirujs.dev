import { Derive } from "kiru"
import { defineHeadContent } from "kiru/router"
import { TabGroup } from "$/components/TabGroup"
import { selectedTab } from "./selectedTab"
import { siteCodeLang } from "$/state"
import ExampleApp from "./example.App.md"
import ExampleAppTS from "./example.App.ts.md"
import ExampleIndex from "./example.index.md"
import { CopyInnerText } from "$/components/CopyInnerText"

export const head = defineHeadContent({
  title: "Kiru - Portal",
  extraMeta: [
    { name: "keywords", content: "portal, component, modal, tooltip, overlay" },
  ],
  description: "Learn how to use Portal component in Kiru to render children outside the application DOM tree, useful for modals, tooltips, and overlays.",
})

# Portal

`Portal` is a component used to escape the application DOM 
tree. Children of the portal will be rendered as a child of the 
container. 

### Example

<div>
  <div className="not-prose">
    <TabGroup
      items={["App", "index.html"]}
      onSelect={(value) => (selectedTab.value = value)}
      value={selectedTab.value}
      itemSuffix={(item) => item === "App" ? `.${siteCodeLang.value}x` : ""}
    />
    <CopyInnerText>
      <Derive from={{selectedTab, siteCodeLang}}>
        {({selectedTab, siteCodeLang}) => {
          return selectedTab === "App" ? 
            siteCodeLang === "js" ? (<ExampleApp />) : (<ExampleAppTS />)
            : (<ExampleIndex />)
        }}
      </Derive>
    </CopyInnerText>
  </div>
</div>